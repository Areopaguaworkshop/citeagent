{
  "name": "@ephremyuan/citeagent",
  "version": "0.5.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "citeagent-install": "dist/bin/install.js",
    "citeagent-mcp-server": "dist/bin/mcp-server.js"
  },
  "files": [
    "dist/",
    "assets/"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=bun --format=esm",
    "build:bin": "bun build bin/install.ts --outfile=dist/bin/install.js --target=bun --format=esm && bun build bin/mcp-server.ts --outfile=dist/bin/mcp-server.js --target=bun --format=esm",
    "build:all": "bun run build && bun run build:bin",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test && bun run build:all",
    "prepublishOnly": "bun run check"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.14.30",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.0",
    "minisearch": "^7.1.0"
  },
  "keywords": [
    "opencode",
    "codex",
    "mcp",
    "academic",
    "citation",
    "merkle",
    "research",
    "citeagent",
    "ai",
    "agents"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/areopaguaworkshop/citation.git",
    "directory": "plugins/opencode-citeagent"
  },
  "license": "MIT",
  "description": "Local, evidence-first academic agent layer for Codex and OpenCode with paper-scoped, Merkle-verified retrieval",
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
